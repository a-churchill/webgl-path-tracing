import { vec4 } from "gl-matrix";

export const CAMERA_DISTANCE = 5.0;
export const CANVAS_ID = "webgl-canvas";
export const MAX_PLANES = 5;
export const MAX_SPHERES = 5;

// colors
export const RED = vec4.fromValues(1.0, 0.5, 0.5, 1.0);
export const GREEN = vec4.fromValues(0.5, 1.0, 0.5, 1.0);
export const BLUE = vec4.fromValues(0.5, 0.5, 1.0, 1.0);
export const WHITE = vec4.fromValues(1.0, 1.0, 1.0, 1.0);
